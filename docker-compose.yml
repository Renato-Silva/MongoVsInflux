# docker-compose.yml
version: "3.3"
services:
    influxdb:
        build: ./influxdb
        hostname: influxdb
        ports:
          - 8086:8086
    mongodb:
        build: ./mongodb
        hostname: mongodb
        ports:
          - 27017:27017
